using System.IO;

namespace Flavo
{
    public static class VariableObjectMenu
    {
        private const string BASE_MENU_PATH = "Variables/";
        private const string BOOL_VAR_MENU_SUFFIX = "Bool Variable";

        public const string BOOL_MENU_PATH = BASE_MENU_PATH + BOOL_VAR_MENU_SUFFIX;
    }
}
